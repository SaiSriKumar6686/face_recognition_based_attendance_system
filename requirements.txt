
# ── Core ─────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
onnx>=1.15.0
onnxruntime>=1.16.0

# ── Face / Vision ────────────────────────────
insightface>=0.7.3
opencv-python>=4.8.0
Pillow>=10.0.0
albumentations>=1.3.1
scikit-image>=0.21.0

# ── Augmentation / GAN helpers ───────────────
# Deep3DFaceRecon — install separately (see README)
# SimSwap       — install separately (see README)
image-quality>=1.2.7          # BRISQUE scorer

# ── Embedding store ──────────────────────────
faiss-cpu>=1.7.4              # switch to faiss-gpu if GPU available

# ── Database / backend ───────────────────────
Flask>=3.0.0
Flask-Login>=0.6.0
Flask-SocketIO>=5.3.0
SQLAlchemy>=2.0.0

# ── Continual learning / math ────────────────
numpy>=1.24.0
scipy>=1.11.0

# ── Utilities ────────────────────────────────
PyYAML>=6.0
tqdm>=4.66.0
loguru>=0.7.0
pytest>=7.4.0
